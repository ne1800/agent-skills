---
interface:
  display_name: "Mise Workflow"
  short_description: "Standardized workflow for repositories using mise"
  default_prompt: >-
    Use this skill when a repository uses mise.toml. Install tools with mise,
    run tasks with mise run, and use mise exec for ad-hoc commands.
